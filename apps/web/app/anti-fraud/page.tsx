
export default function M7(){return <div style={{padding:16,fontFamily:'sans-serif',background:'#FFF1F2'}}><h1>🚨 M7 Anti Fraud CORE - Team Security - PWA iOS+Android</h1><p>fraud_scores 0 - Chặn trước M5/M-Chợ - checkFraud() score&lt;70 allowed - Works both</p><pre style={{background:'#111',color:'#0f0',padding:12,borderRadius:8}}>{`// iOS + Android - Same API
export async function checkFraud({mssv, ip, deviceId, action}){
  const isIOS = /iPhone/.test(deviceId)
  // Same logic both
  return {score: 10, allowed: true}
}`}</pre></div>}
